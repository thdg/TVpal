<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="is.tvpal"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="14" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="is.activites.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="is.activites.DisplayStod2Activity"
            android:label="@string/title_activity_display_stod2" >
        </activity>
        <activity
            android:name="is.activites.DisplayRuvActivity"
            android:label="@string/title_activity_display_ruv" >
        </activity>
        <activity
            android:name="is.activites.DisplaySkjarinnActivity"
            android:label="@string/title_activity_display_skjarinn" >
        </activity>
        <activity
            android:name="is.activites.DetailedEventActivity"
            android:label="@string/title_activity_detailed_event" >
        </activity>
    </application>

</manifest>
